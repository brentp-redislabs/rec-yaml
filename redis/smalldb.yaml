apiVersion: app.redislabs.com/v1alpha1
kind: RedisEnterpriseDatabase
metadata:
  name: testdb
spec:
  databaseSecretName: test-secret
  memorySize: 100MB
  shardCount: 1
  replication: false
  tlsMode: enabled
